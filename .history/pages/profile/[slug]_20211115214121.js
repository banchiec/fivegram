import React from 'react'

export default function Proyects({ proyects }) {
    return (
        <div>
            {
                proyects.map((proyect) => {

                    <h2>details</h2>
                })
            }
            {console.log(slug)}
        </div>
    )
}
